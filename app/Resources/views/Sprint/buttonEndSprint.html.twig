{% if sprint.started %}
    {% set endSprintForm = endSprintForm(sprint) %}
    {% form_theme endSprintForm with [':form:backlog_bootstrap_inline.html.twig', _self] %}
    {{ form(endSprintForm) }}
{% endif %}
